# Always publish as public
access=public

# Save exact versions in package.json
save-exact=true

# Use consistent casing for scoped packages
strict-peer-dependencies=true

# Ensure reproducible installs
package-lock=true

# Set default registry
registry=https://registry.npmjs.org/

# Always use the latest when installing
tag=latest
